name: 'Reusable workflow matrix'

on:
  push:

jobs:

  use:
    strategy:
      matrix:
        workflow:
          - variant1
    uses: ./.github/workflows/reusable-workflow-matrix-${{ matrix.workflow }}.yml
