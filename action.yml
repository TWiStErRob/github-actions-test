name: "test"
description: "test"
inputs:
  some:
    required: false
    description: "Some"
runs:
  using: composite
  steps:
    - name: "Debug"
      env:
        SOME: ${{ inputs.some }}
      run: echo "${SOME}"
